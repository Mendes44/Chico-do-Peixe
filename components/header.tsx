'use client';
import Link from 'next/link';
import { Menu, X } from 'lucide-react';
import { usePathname } from 'next/navigation';
import { useEffect, useState } from 'react';
const links = [{ href: '/', label: 'Início' }, { href: '/cardapio', label: 'Cardápio' }, { href: '/sobre', label: 'Nossa história' }, { href: '/contato', label: 'Contato' }];
/* Menu responsivo: fecha na troca de rota e evita rolagem do fundo quando aberto. */
export function Header() { const [open, setOpen] = useState(false); const pathname = usePathname(); useEffect(() => setOpen(false), [pathname]); useEffect(() => { document.body.style.overflow = open ? 'hidden' : ''; return () => { document.body.style.overflow = ''; }; }, [open]); return <header className="site-header"><div className="container header-inner"><Link className="brand" href="/" aria-label="Chico do Peixe — início"><img src="/images/logo.png" alt="" width="62" height="48" /><span>CHICO <small>DO PEIXE · SANTA INÊS</small></span></Link><nav className="desktop-nav" aria-label="Navegação principal">{links.map(link => <Link key={link.href} className={pathname === link.href ? 'active' : ''} href={link.href}>{link.label}</Link>)}<a className="button button-small" href="https://wa.me/5531991536420" target="_blank" rel="noopener noreferrer">Reservar</a></nav><button className="menu-toggle" type="button" aria-expanded={open} aria-controls="mobile-menu" aria-label={open ? 'Fechar menu' : 'Abrir menu'} onClick={() => setOpen(!open)}>{open ? <X /> : <Menu />}</button></div><nav id="mobile-menu" className={`mobile-nav ${open ? 'open' : ''}`} aria-label="Navegação móvel">{links.map(link => <Link key={link.href} href={link.href}>{link.label}</Link>)}<a className="button button-primary" href="https://wa.me/5531991536420" target="_blank" rel="noopener noreferrer">Reservar pelo WhatsApp</a></nav></header>; }
